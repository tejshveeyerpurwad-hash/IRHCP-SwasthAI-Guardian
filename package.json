{
  "name": "swasthai-guardian",
  "version": "1.0.0",
  "engines": {
    "node": "20.14.0"
  },
  "private": true,
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd Swasthai-Guardian-Up/frontend && npm install && npm run build",
    "start": "node Swasthai-Guardian-Up/backend/server.js"
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "heroku-postbuild": "npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
